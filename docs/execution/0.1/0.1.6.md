# Execution Log 0.1.6

## Scope

- Goal: Combat feedback and quick-access fun pass
- Iteration: 0.1.x
- Status: partially-delivered-in-baseline

## Changes

- Planned outputs: Combat readability package (TrueHUD) + quick action access candidate (Wheeler, optional)
- Tracked files: `baseline/modlist.txt`, `baseline/plugins.txt`

### Baseline evidence

- `TrueHUD - HUD Additions` is present in `baseline/modlist.txt` (UI_separator group).
- `TrueHUD.esl` is active in `baseline/plugins.txt` (line 10).
- TrueHUD entered the baseline in commit `f0f187d` (2026-02-23, Milestone 10 UI/HUD pass era).

### What is not yet done

- `Wheeler` (optional quick-access wheel) is not present in the baseline. It remains a candidate, not yet installed.

## Validation

- Definition of done: Combat info is more legible and quick-access flow is stable in playtest
- Evidence links: `baseline/modlist.txt`, `baseline/plugins.txt`, commit `f0f187d`
- Test notes: pending. TrueHUD's enemy info bars and target data should be playtested for legibility and FPS impact alongside `SoulsyHUD` and `Compass Navigation Overhaul` (HUD overlap cluster).

## Risks

- HUD overlap risk: TrueHUD + SoulsyHUD + Compass Navigation Overhaul all draw HUD elements. See open risk in `docs/maintenance/2026-03-09-baseline-maintenance.md`.
- Wheeler, if added later, introduces a keybind and input layer that must be checked against the existing control map.

## Rollback

- Disable `TrueHUD.esl` in MO2. If Wheeler is later added, disable both and remove any Wheeler-related INI/keybind entries.

## Links

- Iteration plan: docs/iterations/0.1.md
- Roadmap: docs/roadmap.md
- Technical release note target: docs/releases/0.2.0.md
